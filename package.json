{
  "name": "property-management-app",
  "version": "1.0.0",
  "description": "Property Management Application",
  "main": "server/index.js",
  "scripts": {
    "build": "cd client-angular && npm install && npm run build && cd .. && cd server && npm install && cd ..",
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongodb": "^5.9.2",
    "node-fetch": "^2.6.7",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
